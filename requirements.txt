# ============================================================
# RabMapper - dependencias
# Python 3.10.x (OBRIGATORIO no Windows)
#
# Por que 3.10: flare-floss depende de binary2strings, uma extensao C++ que
# so publica wheel para cp310 no Windows. Em 3.11/3.12/3.13/3.14 o pip tenta
# compilar do zero e falha sem o Visual Studio Build Tools.
# ============================================================

# --- Analise estatica / extracao ---
flare-floss>=3.1.0,<4                  # extracao de strings (estaticas, stack, decodificadas)
pefile>=2023.2.7            # parsing de binarios PE (imports, secoes, entropia)
yara-python>=4.5.0          # compilacao e validacao de regras YARA

# --- Threat Intelligence ---
mitreattack-python>=3.0.6   # MITRE ATT&CK oficial via STIX 2.x
cvss>=3.2                   # calculo de score CVSS 3.1 / 4.0

# --- Enriquecimento (APIs externas) ---
requests>=2.32.0            # clients HTTP (VirusTotal, Shodan, MalwareBazaar)
pyzipper>=0.3.6             # ZIP com AES: formato em que o MalwareBazaar entrega amostra

# --- Configuracao ---
python-dotenv>=1.0.1        # carrega API keys do .env

# --- Relatorios ---
reportlab>=4.2.0            # geracao de PDF
python-docx>=1.1.2          # geracao de DOCX

# --- Interface grafica ---
PySide6>=6.7.0              # GUI desktop (Qt6)

# --- Dev / testes ---
pytest>=8.3.0
